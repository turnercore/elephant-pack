Always update the extension version when changing user-facing behavior. Keep `package.json`, `package-lock.json`, and `public/manifest.json` in sync, then regenerate Safari assets so the Safari extension manifest matches.

Use context7 to check current documentation of game engines or code languages when needed.
